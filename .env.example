# Repository-level defaults for local development

BACKEND_BASE_URL=http://127.0.0.1:8000
# 逗号分隔；浏览器直连 API（合同 PDF 等）时需允许 web-admin 来源。生产建议改为具体域名。
CORS_ALLOW_ORIGINS=*
DATABASE_URL=sqlite:///./reconciliation.db
